import React from 'react';

interface IMeterProps {}

export const Meter: React.FunctionComponent<IMeterProps> = (props) => {
    const {} = props;

    /** Override Defaults */
    return <div>Meter</div>;
};
